version: 2
formats: []

build:
  image: latest
  os: ubuntu-20.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: false
  builder: html

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs